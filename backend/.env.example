PORT=4000
MONGO_URI=mongodb://127.0.0.1:27017/pollDB
JWT_SECRET=supersecretkey123
JWT_EXPIRES_IN=7d
# Comma-separated frontend URLs (REST + Socket.IO CORS). No trailing slash.
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,https://your-app.vercel.app
# Optional: allow any *.vercel.app preview URL (branch deploys)
# ALLOW_VERCEL_PREVIEWS=true
